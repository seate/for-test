spring:
    application:
        name: yogerOrder

    jpa:
        #show-sql: true
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                format_sql: true